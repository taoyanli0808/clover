{
  "name": "clover",
  "version": "1.0.0",
  "description": "vue front for clover platform.",
  "author": "taoyanli0808",
  "private": true,
  "scripts": {
    "dev": "cross-env BASE_URL=http://127.0.0.1:5000 NODE_ENV=development nuxt",
    "build": "cross-env BASE_URL=http://127.0.0.1:5000 NODE_ENV=production nuxt build",
    "start": "cross-env BASE_URL=http://127.0.0.1:5000 NODE_ENV=production nuxt start",
    "generate": "cross-env BASE_URL=http://127.0.0.1:5000 NODE_ENV=production nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore ."
  },
  "dependencies": {
    "nuxt": "^2.0.0",
    "element-ui": "^2.4.11",
    "@nuxtjs/axios": "^5.3.6"
  },
  "devDependencies": {
    "@nuxtjs/eslint-config": "^1.0.1",
    "@nuxtjs/eslint-module": "^1.0.0",
    "babel-eslint": "^10.0.1",
    "cross-env": "^6.0.3",
    "eslint": "^6.1.0",
    "eslint-plugin-nuxt": ">=0.4.2"
  }
}
