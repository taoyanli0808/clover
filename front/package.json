{
  "name": "clover",
  "version": "1.0.0",
  "description": "vue front for clover platform.",
  "author": "taoyanli0808",
  "private": true,
  "scripts": {
    "dev": "cross-env BASE_URL='http://127.0.0.1:5000' nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "cross-env BASE_URL='http://127.0.0.1:5000' nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore ."
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.3.6",
    "cross-env": "^6.0.3",
    "element-ui": "^2.4.11",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "@nuxtjs/eslint-config": "^1.0.1",
    "@nuxtjs/eslint-module": "^1.0.0",
    "babel-eslint": "^10.0.1",
    "eslint": "^6.1.0",
    "eslint-plugin-nuxt": ">=0.4.2"
  }
}
